# 지옥 카운트다운 레이싱 게임 구현 체크리스트

> **원본 GDD:** `C:\Users\kkkkk\Downloads\지옥_카운트다운_레이싱_게임_GDD.md`  
> **GDD 최종 수정일:** 2026년 7월 24일  
> **체크리스트 최종 리뷰일:** 2026년 7월 26일  
> **구현 현황 기준:** `origin/develop` / `39bd078` (코드·에셋 실물 확인 + memento 기록 대조)  
> **엔진:** Unity `6000.3.19f1`  
> **플랫폼:** Windows PC, WebGL  
> **목표:** 3~5분 안에 끝나는 완성도 높은 단일 레이스

## 체크 규칙

- `[x]` 현재 프로젝트에 구현되어 있고 기반으로 사용할 수 있음
- `[ ]` 새로 구현하거나 GDD에 맞게 수정·검증해야 함
- 부분 구현은 완료로 표시하지 않고 **수정 필요**에 둔다.
- 완료 체크는 코드 작성, Play Mode 확인, 관련 테스트 통과까지 포함한다.

---

## 0. GDD 기준 확정 목표

| 항목 | 구현 목표 |
|---|---|
| 코스 | 목표 지점까지 달리는 1랩 레이스 |
| 참가 차량 | 플레이어 1대 + AI 5대, 총 6대 |
| 설정 | 차량 수, 탈락 간격 등 주요 수치를 설정으로 분리 |
| 탈락 | 30초마다 현재 최하위 차량 처형 |
| 탈락 종료 | 두 대가 남으면 탈락 중단 |
| 최종 단계 | 남은 두 대가 닫히는 관문을 향해 질주 |
| 승리 | 관문을 먼저 통과한 차량 |
| 차량 조작 | 가속·제동·조향·충돌·부스트·기본 드리프트 |
| 스킬 | 파열, 엔진 봉인, 영혼 교환 |
| 퀴즈 | 레이스를 멈추지 않고 마우스로 답 선택 |
| 추월 도전 | 20초 주기 후보 판정, 8초 안에 추월 후 0.5초 유지 |
| 특수 이벤트 | 덤프트럭 습격, 지진, 운석 낙하 |
| 처형 화면 | 처형 차량을 메인 화면에 표시하고 플레이어 화면은 오른쪽 아래 1/3 크기로 축소 |
| 결과 | 빠른 승리·패배 표시와 재시작 |

### GDD 내부 불일치

- 최신 MVP의 `1랩`을 우선하고, 트랙 설명의 `3랩 서킷`은 이전안으로 본다.
- 최신 MVP와 기준표의 `총 6대`를 우선하고, 완료 정의의 `차량 5대 단계`는 `차량 6대 단계`로 해석한다.
- 퀴즈 제한 시간이 기준표에는 `3초`, 발동 흐름에는 `4초`로 적혀 있으나 설정 기본값은 `4초`로 확정한다.
- 기본 드리프트는 MVP에 포함하되 고급 드리프트와 트릭 시스템은 제외한다.

### 모든 수치 설정화 원칙

문서에 적힌 게임플레이 숫자는 **코드 상수가 아니라 기본 프리셋 값**이다. 날짜, Unity 버전, 현재 테스트 개수처럼 빌드 사실을 설명하는 숫자를 제외한 모든 런타임·밸런스·연출 수치는 코드 수정 없이 Unity Inspector에서 변경할 수 있어야 한다.

- [x] 단일 루트 `GameBalanceSettings` ScriptableObject 생성
- [ ] 설정 그룹 분리 — 구현됨: `RaceSettings`, `EliminationSettings`, `ResultSettings`, `QuizSettings`, `CurseSettings`, `BoostSettings`, `DamageSettings`, `OvertakeSettings`, `CameraSettings`, `AISettings`(+`AiDrivingSettings`, `AiPersonalityAssignmentSettings`), `TrackValidationSettings`, `PresentationSettings`, `PlaytestSettings` / **미구현: `VehicleSettings`, `SpecialEventSettings`** (차량 수치는 RCCP 프리팹, 특수 이벤트 수치는 `RandomEventManager` 인스펙터에 남아 있음)
- [x] 게임 잼 기본값을 담은 `GameJamDefault` 프리셋 에셋 생성 — `Assets/Resources/GameBalance/GameJamDefault.asset`
- [x] 빠른 자동 테스트용 `FastTest` 프리셋 에셋 생성 — `Assets/Resources/GameBalance/FastTest.asset`
- [x] 런타임 시작 시 선택된 설정을 불변 스냅샷으로 만들어 모든 시스템에 공급 — `GameBalance.Current` / `GameBalance.SetActive`
- [ ] 레이스 관리자, 차량, AI, HUD, 카메라, VFX, 오디오가 같은 설정 스냅샷을 참조
- [ ] MonoBehaviour와 프리팹에 동일 수치를 중복 직렬화하지 않음
- [ ] 정적 상수, 메서드 내부 리터럴, 코루틴 대기시간에 밸런스 숫자를 직접 작성하지 않음
- [x] 테스트도 숫자를 다시 하드코딩하지 않고 테스트 프리셋 또는 설정 스냅샷을 사용
- [ ] 설정값 변경이 재시작 후 새 레이스에 반영되고 코드 재컴파일은 요구하지 않음
- [ ] 빌드에서도 Resources 또는 명시적 프리셋 참조를 통해 설정을 로드
- [ ] 설정 누락 시 조용히 임의 기본값을 만들지 않고 명확한 검증 오류 출력

### 설정 카탈로그와 GDD 기본값

| 설정 그룹 | 설정 키 | GDD 기본값 또는 상태 |
|---|---|---:|
| Race | `aiCount` | 5 |
| Race | `lapCount` | 1 |
| Race | `finalDuelRacerCount` | 2 |
| Race | `targetRaceDurationMinSeconds` | 180 |
| Race | `targetRaceDurationMaxSeconds` | 300 |
| Elimination | `intervalSeconds` | 30 |
| Elimination | `warningSeconds` | 10 |
| Elimination | `intenseWarningSeconds` | 5 |
| Elimination | `executionCameraLeadSeconds` | 3 |
| Elimination | `executeAtRemainingSeconds` | 0 |
| Result | `playerDeathCinematicMaxSeconds` | 3 |
| Result | `restartToControlMaxSeconds` | 5 |
| Quiz | `answerTimeSeconds` | 4 |
| Quiz | `minimumAnswerCount` | 2 |
| Quiz | `maximumAnswerCount` | 3 |
| Curse | `sharedCooldownSeconds` | 12 |
| Boost | `maximumCharges` | 2 |
| Boost | `durationSeconds` | 1.25 |
| Boost | `rechargeSecondsPerCharge` | 6 |
| Boost | `overtakeRewardCharges` | 1 |
| Damage | `maximumDurability` | 100 |
| Damage | `damagedThreshold` | 60 |
| Damage | `criticalThreshold` | 30 |
| Damage | `wreckedThreshold` | 0 |
| Damage | `wreckDurationSeconds` | 2 |
| Damage | `recoveryDurability` | 50 |
| Damage | `recoveryProtectionSeconds` | 1.5 |
| Rupture | `durabilityDamage` | 35 |
| Rupture | `knockbackVelocityChange` | 3 |
| Engine Seal | `durationSeconds` | 5 |
| Soul Swap | `minimumDistanceMeters` | 15 |
| Soul Swap | `maximumDistanceMeters` | 40 |
| Soul Swap | `collisionIgnoreSeconds` | 0.3 |
| Overtake | `checkIntervalSeconds` | 20 |
| Overtake | `challengeDurationSeconds` | 8 |
| Overtake | `requiredLeadHoldSeconds` | 0.5 |
| AI Curse | `warningSeconds` | 1 |
| AI Curse | `hostileEffectGraceSeconds` | 2 |
| Camera | `sideBySideActivationSeconds` | 0.6 |
| Camera | `executionPlayerViewportRect` | 오른쪽 아래 약 1/3, Rect로 설정 |
| Camera | `executionTransitionSeconds` | 0.2 |
| Camera | `executionReturnSeconds` | 0.25 |
| Gate | `closeDurationSeconds` | 0.75 |
| Gate | `loserExecutionDelaySeconds` | 0.25 |
| Special Event | `enabled` | true |
| Special Event | `initialDelaySeconds` | 15 |
| Special Event | `minimumIntervalSeconds` | 25 |
| Special Event | `maximumIntervalSeconds` | 40 |
| Special Event | `warningLeadSeconds` | 2 |
| Special Event | `maximumSimultaneousEvents` | 1 |
| Special Event | `executionSafetySeconds` | 10 |
| Special Event | `maximumEventsPerRace` | 4 |
| Dump Truck | `speedMetersPerSecond` | 22 |
| Dump Truck | `durabilityDamage` | 20 |
| Dump Truck | `knockbackVelocityChange` | 6 |
| Dump Truck | `lifetimeSeconds` | 12 |
| Earthquake | `durationSeconds` | 3 |
| Earthquake | `lateralVelocityChange` | 2 |
| Earthquake | `cameraShakeIntensity` | 0.35 |
| Meteor | `impactDelaySeconds` | 2 |
| Meteor | `impactRadiusMeters` | 5 |
| Meteor | `durabilityDamage` | 25 |
| Meteor | `knockbackVelocityChange` | 5 |
| Meteor | `effectLifetimeSeconds` | 3 |
| Track Validation | `minimumBoostStraights` | 2 |
| Track Validation | `minimumParallelSections` | 1 |
| Playtest | `ruleUnderstandingTargetSeconds` | 15 |
| Playtest | `minimumExternalTesterCount` | 3 |

플레이어 1명은 조정 가능한 밸런스 숫자가 아니라 싱글플레이 게임 모드의 구조적 불변조건이다. `totalRacerCount`는 별도로 저장하지 않고 `1 + aiCount`로 계산한다.

### 숫자 외 정책 설정

GDD에서 선택지가 열려 있는 동작도 코드 분기 수정 없이 프리셋에서 고를 수 있게 한다.

| 설정 그룹 | 설정 키 | 권장 기본값 | 상태 |
|---|---|---|---|
| Curse | `selectionMode` | `OrderedRotation` | 확정 |
| Curse | `targetingMode` | `NearestAheadThenNearestActive` | 확정 |
| Overtake | `cooldownRewardMode` | `Reset` | 확정 |
| Overtake | `cooldownReductionSeconds` | 6 | 대체 `Reduce` 모드용 |
| AI | `archetypeAssignments` | 난폭자 1, 스피드스터 2, 책략가 1, 생존자 1 | 확정 |
| Quiz | `language` | `English` | 확정 |
| Special Event | `blockDuringExecutionWarning` | true | 확정 |
| Special Event | `blockDuringQuiz` | true | 확정 |
| Special Event | `blockDuringOvertakeChallenge` | true | 확정 |
| Special Event | `blockDuringFinalDuel` | true | 확정 |
| Special Event | `cancelActiveOnProtectedPhase` | true | 확정 |
| Special Event | `preventImmediateRepeat` | true | 확정 |

### 설정 유효성 검증

- [ ] `1 + aiCount`로 총 차량 수를 계산하고 총 차량 수를 별도 중복 저장하지 않음
- [ ] 모든 시간·거리·피해·충전 수치는 0 이상인지 검사
- [ ] `minimumDistanceMeters <= maximumDistanceMeters` 검사
- [ ] `minimumAnswerCount <= maximumAnswerCount` 검사
- [ ] 피해 단계가 `maximumDurability > damagedThreshold > criticalThreshold > wreckedThreshold` 순서인지 검사
- [ ] 모든 탈락 경고 시간이 `intervalSeconds`보다 작은지 검사
- [ ] `finalDuelRacerCount`가 전체 차량 수보다 작은지 검사
- [ ] 부스트 회복량이 최대 충전량을 넘지 않도록 클램프
- [ ] 카메라 Viewport Rect가 화면 범위 안에 있는지 검사
- [ ] `minimumIntervalSeconds <= maximumIntervalSeconds` 검사
- [ ] `maximumSimultaneousEvents`와 `maximumEventsPerRace`가 1 이상인지 검사
- [ ] 특수 이벤트 정의의 프리팹·경고 VFX·오디오 참조 누락 검사
- [ ] 잘못된 설정은 `OnValidate`와 런타임 시작 검증에서 구체적인 필드명과 함께 보고

---

## 1. 구현 현황 스냅샷 (2026-07-26)

> 아래 표는 `origin/develop` `39bd078`의 **코드·에셋 실물**과 팀 기록(memento)을 대조해 정리했다.
> 검증 열은 `테스트` = EditMode 자동 테스트 통과, `플레이` = 사람이 Play Mode에서 확인, `컴파일` = 컴파일·정적 확인만.

| 시스템 | 상태 | 근거 | 검증 |
|---|---|---|---|
| 설정 시스템 (`GameBalanceSettings` + 프리셋 2종 + 스냅샷 공급) | 구현 | `Assets/GameBalance/Runtime/GameBalanceSettings.cs`, `GameBalance.cs`, `Assets/Resources/GameBalance/{GameJamDefault,FastTest}.asset` | 테스트 |
| 레이스 상태 흐름 (`Boot→PreRace→Countdown→Racing→FinalDuel→Finished`) | 구현 | `GMTKRaceState.cs`, `RaceFlow.cs`, `RaceBootstrap.cs` | 플레이 |
| 체크포인트·순위·랩 | 구현 (킷 인프라 유지) | 킷 `Checkpoints`/`CheckpointTracker`/`RealTimeRacePositions` + `Race.cs` 어댑터 | 플레이 |
| 주행 진행도 측정 (경로 arc-length) | 1단계 — 측정·비교만, 순위 공급은 아직 체크포인트 | `TrackProgress.cs`, `GmtkRaceProgress.cs` | 테스트 |
| 탈락 (30초 간격 최하위 처형) | 구현 | `EliminationManager.cs`, `CarExplosion.cs`, `EliminationSettings` | 플레이 |
| 최종 관문 | 구현 | `FinalGate.cs`, `PresentationSettings` | 플레이 |
| 내구도·대파 | 구현 (상태머신) | `DurabilityController.cs`, `DurabilityManager.cs`, `DurabilityState.cs`, `DurabilityHud.cs` | 테스트 |
| 부스트 (2칸·1.25초·6초 재충전, Space/Shift) | 구현 | `BoostController.cs`, `BoostManager.cs`, `BoostState.cs` | 테스트 |
| 저주 3종 (파열·엔진 봉인·영혼 교환) + 공용 쿨다운 | 구현 | `CurseController.cs`, `CurseManager.cs`, `CurseType.cs`, `CurseCooldownState.cs` | 테스트 |
| 추월 도전 (20초 주기·8초 제한·0.5초 유지) | 구현 | `OvertakeManager.cs`, `OvertakeChallengeState.cs` | 테스트 |
| 퀴즈 + 미니게임 4종 (산술·수도·숫자 순서·리듬·버튼 연타) | 구현 | `Assets/Quiz/Runtime/*` (16파일), 휴대폰 월드 UI | 테스트·플레이 |
| 주행 특수 이벤트 5종 (공사 구간·가축 횡단·비치볼·운석·지진) | 구현 | `RandomEventManager.cs` | 플레이 |
| 차량 계층 (RCCP Lite) | 구현 | `Assets/Scripts/GMTK/Rccp/*`, `GmtkVehicleAdapter.cs` | 플레이 |
| AI 주행 (look-ahead·레이싱 라인·코너 속도·그리드 합류) | 구현 | `GmtkRccpWaypointDriver.cs`, `GmtkRccpWaypointPath.cs`, `AiDriving.cs` | 테스트·플레이 |
| AI 성격 배분 (시드 셔플·균등 구성) | 구현 | `AIPersonalityAssigner.cs`, `AiPersonalityRoster.cs` | 테스트 |
| 레이스 씬·트랙 (1,426m 서킷, 체크포인트 8, 그리드 14) | 구현 | `Assets/Scenes/GMTK_Race.unity`, `Assets/Track/*` | 플레이 |
| HUD (순위·처형 카운트다운·부스트·저주·도전·관문) | 구현 | `RaceHud.cs`, `RaceHud.prefab` | 플레이 |
| 시작 화면·오디오 | 구현 | `StartMenuCanvas.cs`, `GameAudioManager.cs`, `Assets/Sound/*` (47) | 플레이 |
| 추락 리스폰 | 작업 중 (미커밋) | `GmtkRccpFallRespawner.cs`, `FallRespawnState.cs` | 컴파일 |
| 처형 화면 분할 (메인=처형 대상, 플레이어=우하단 1/3) | **미구현** | `CameraSettings.executionPlayerViewportRect`만 존재, 뷰포트 전환 코드 없음 | — |
| `VehicleSettings`·`SpecialEventSettings` 설정화 | **미구현** | 차량 수치는 RCCP 프리팹, 이벤트 수치는 컴포넌트 인스펙터에 산재 | — |
| 팀 UI ↔ 레이스 씬 통합 | **미완** | `RaceHud`/`StartScreen` 프리팹은 `Assets/Scenes/ui.unity`에만 배치, `GMTK_Race`에는 없음 | — |

### 자동 테스트 현황

`Assets/GameBalance/Tests/Editor` 9파일 + `Assets/Quiz/Tests/Editor` 5파일 = **14개 테스트 파일**
(`BoostState`, `CurseCooldownState`, `DurabilityState`, `OvertakeChallengeState`, `FallRespawnState`, `GameBalanceSettings`, `AiDriving`, `AiPersonalityRoster`, `TrackProgress`, `ButtonMashProgress`, `NumberSequenceProgress`, `QuizQuestion`, `QuizSessionController`, `WorldSpaceQuizCanvasFollower`)

### 알려진 미해결 항목

- **`GMTK_Race.unity`의 체크포인트 게이트가 재베이크 대기 중** — 생성기 버그(게이트당 `Checkpoint` 컴포넌트 2개, 그중 1개는 참조 미배선)를 `RaceTrackAuthoring.BuildCheckpoints`에서 고쳤으나, 이미 베이크된 씬은 고쳐지지 않는다. 현재 씬은 게이트 467개에 컴포넌트 934개·미배선 467개로, 매 프레임 NRE가 나고 랩 판정용 게이트 수가 2배로 집계된다. 씬 소유자가 **Bake Track**을 한 번 눌러야 해소된다
- 처형 화면 분할 연출 미구현 (GDD 시그니처 요소)
- 팀 HUD가 레이스 씬에 배치되지 않아 `GMTK_Race` 플레이 시 킷 UI만 표시됨
- `GameJamDefault.maximumDurability = 1000000` — 대파를 미루기 위한 임시값, 밸런스 확정 시 되돌려야 함
- RCC 차량 렌즈 플레어가 URP에서 표시되지 않음 (`LegacyLensFlareUrpBridge` 미검증)
- `GMTK_Race.unity`가 바이너리 직렬화 상태 — 씬 충돌 시 스마트 머지 불가

---

## 2. 기존 구현 세부 (RCCP 전환 전 기준)

### 2.0 차량과 레이스 기반 (킷 기준 항목)


- [x] 플레이어 차량 가속·제동·후진·조향
- [x] 핸드브레이크 입력
- [x] 휠과 노면 기반 차량 물리
- [x] 차량 간 충돌
- [x] 스키드와 타이어 마찰 기반
- [x] 체크포인트 순서 검증
- [x] 랩 진행과 완주 처리
- [x] 랩·체크포인트·구간 거리 기반 실시간 순위
- [x] 플레이어와 AI 차량 스폰
- [x] 출발 카운트다운
- [x] 기본 승리·패배 결과 화면
- [x] 일시정지와 기본 재시작

### 2.1 AI 기반 (킷 AI는 폐기, RCCP 드라이버로 대체)

- [x] AI 웨이포인트 주행 — RCCP `GmtkRccpWaypointDriver`로 재구현 (킷 `CarAIControl` 폐기)
- [x] 코너 진입 감속 — 반경 기반 목표 속도 (`AiDriving.CornerSpeedKph`)
- [ ] 전방 장애물 감지와 회피 조향 — **킷 AI 기능이라 RCCP 전환으로 사라짐, 재구현 필요**
- [x] 정체 시 후진 복구 — `GmtkRccpWaypointDriver.UpdateRecovery`
- [ ] 장시간 복구 실패 시 체크포인트 리스폰 — `GmtkRccpFallRespawner` 작업 중(미커밋)
- [x] AI 성격별 속도와 주행 성향 적용 구조 — 페이스 배수만 반영(0.86~1.0), **차이가 미미해 확대 필요**
- [x] `CleanRacer`, `Rammer`, `Blocker`, `Reckless` 성격

### 2.2 탈락

- [x] 현재 최하위 차량 검색
- [x] 시간 기반 최하위 차량 제거
- [x] 플레이어 탈락 시 패배 처리
- [x] AI 차량 탈락 처리
- [x] 탈락 차량 제어 비활성화
- [x] 폭발 물리 힘과 회전
- [x] 간단한 폭발 플래시와 발광 효과
- [x] 일정 시간 후 잔해 비활성화

### 2.3 퀴즈 시스템

- [x] 데이터 기반 문제와 문제 풀
- [x] 무작위 문제 선택
- [x] 정답·오답·시간 초과 판정
- [x] 월드 스페이스 휴대폰 UI
- [x] TextMeshPro 기반 문제와 답안 표시
- [x] 마우스 답안 선택
- [x] 수학·수도·숫자 순서·리듬·버튼 연타 문제
- [x] 퀴즈 시작·판정·종료 이벤트
- [x] 퀴즈 EditMode 테스트 18개

### 2.4 카메라·UI·빌드 기반

- [x] 기본 3인칭 추격 카메라 — RCCP `RCCP_Camera` (킷 `PlayerCarCameraController` 폐기)
- [x] 차량 속도 기반 기본 추종
- [ ] 복수 카메라 전환 — 킷 메뉴·피니시 카메라만 남음, 처형용 전환 미구현
- [x] 기본 순위·랩·출발 카운트다운 UI
- [x] Unity `6000.3.19f1` 고정
- [x] Windows 64-bit와 WebGL 빌드 검증
- [x] 활성 씬과 Git LFS 검증
- [x] 레이싱 헤드리스 및 퀴즈 Play Mode 검증

---

## 2. 구현되어 있지만 GDD에 맞게 수정해야 하는 내용

### 2.1 레이스 설정

현재 기본값은 플레이어 1대, AI 2대, 2랩이다.

- [ ] 플레이어 1대와 AI 5대로 변경
- [ ] 총 참가 차량 6대로 변경
- [ ] 목표 지점까지 달리는 1랩으로 변경
- [ ] 문서에 등장하는 모든 런타임·밸런스·연출 수치를 `GameBalanceSettings`로 이전
- [ ] 게임 잼 빌드에서 레이스 설정 선택 UI 숨김
- [ ] 재시작 후에도 같은 설정 유지

### 2.2 조작감과 드리프트

현재 차량 물리와 핸드브레이크 기반은 있지만 완성된 아케이드 드리프트와 안정화가 없다.

- [ ] 저속 조향 보조와 고속 조향 감쇠
- [ ] 측면 충돌 후 트랙션 안정화
- [ ] 의도치 않은 스핀 억제
- [ ] 반대 방향을 향했을 때 빠른 방향 복구
- [ ] 관대한 트랙 이탈 리셋
- [ ] 기본 아케이드 드리프트 진입
- [ ] 드리프트 중 조향과 횡방향 미끄러짐 조절
- [ ] 드리프트 종료 후 빠른 그립 복구
- [ ] AI 차량 질량과 충격 전달 조정

### 2.3 탈락 규칙

현재 첫 탈락은 20초, 이후 12초 간격이며 한 대만 남을 때까지 진행된다.

- [ ] 기존 `EliminationManager` 내부 수치를 `EliminationSettings` 참조로 교체
- [ ] 첫 탈락과 이후 탈락을 모두 30초 간격으로 변경
- [ ] 10초 전 최하위 경고 표식
- [ ] 5초 전 시각·오디오 강도 증가
- [ ] 3초 전 처형 화면 연출 시작
- [ ] 0초에 최신 순위 재검증 후 최하위 처형
- [ ] 두 대가 남으면 탈락 중단
- [ ] 두 대가 남으면 최종 관문 단계로 전환
- [ ] 한 대만 남았을 때 자동 승리하는 기존 규칙 제거

### 2.4 순위와 진행도

- [ ] 순위표·탈락·저주·위치 교환·추월·리스폰이 같은 진행도 사용
- [ ] 체크포인트 구간 진행 거리 정밀화
- [ ] 탈락 차량 순위 제외
- [ ] 탈락 직전 순위 강제 갱신
- [ ] 동률 시 트랙상 앞선 차량 우선
- [ ] 완전 동률이면 이전 순서 유지
- [ ] 안전한 트랙 위치·방향·횡방향 위치 제공

### 2.5 퀴즈 실행 방식

현재 퀴즈는 저주 대상이 인간 플레이어일 때만 실행되며, 정답이면 저주를 방어하고
오답 또는 시간 초과면 페널티를 받는다.

- [ ] 퀴즈 시간·답안 수·출제 간격을 `QuizSettings` 참조로 교체
- [x] 레이스 중 자동 퀴즈 실행 중단
- [x] 저주 활성화 시에만 퀴즈 시작
- [x] 퀴즈 중 레이스와 차량 물리를 멈추지 않음
- [ ] 레이스 문제를 짧은 문제와 답안 2~3개로 제한
- [x] 마우스 입력을 주 입력으로 유지
- [x] 정답 시 저주 페널티 없이 방어 성공
- [x] 오답·시간 초과 시 저주 페널티 적용
- [x] 저주 발동 시 시전자 쿨다운 시작
- [x] 기존 영어 문제 표시 정책 유지

### 2.6 AI 성격과 행동

- [ ] 속도·공격성·회피·부스트·저주·따라잡기 수치를 `AISettings`에 성격별로 저장
- [ ] `Rammer`를 난폭자로 정리
- [ ] 기존 성격을 기반으로 스피드스터 구성
- [ ] 저주 중심 책략가 추가
- [ ] 회피와 순위 방어 중심 생존자 추가
- [ ] AI 5대에 네 가지 성격 배분
- [ ] 직선과 긴급 순위 회복 시 부스트 사용
- [ ] 탈락 카운트다운 반응
- [ ] 단순화된 저주 전술
- [ ] 멀리 뒤처진 AI에 작은 가속 보정
- [ ] 플레이어가 최하위일 때 AI 공격성 감소
- [ ] 최종 관문 라이벌 강화
- [ ] 눈에 띄는 순간이동 금지

### 2.7 승패·재시작·특수 이벤트

- [ ] 기본 랩 완주 승리를 관문 통과 승리로 변경
- [ ] 중앙 레이스 관리자만 승패 판정
- [ ] 플레이어 폭발 후 3초 이내 패배 화면
- [ ] 패배부터 재조작까지 5초 이내 유지
- [ ] 재시작 시 차량·AI·타이머·퀴즈·저주·도전·관문 초기화
- [ ] `RaceFinish`의 Finish Trigger 참조 안전성 검증
- [ ] 현재 `RandomEventManager`를 설정 기반 `SpecialEventDirector`로 리팩터링
- [ ] 기존 지진과 운석 로직은 재사용 가능한 부분만 유지
- [ ] 덤프트럭 습격 이벤트 추가
- [ ] 소·비치볼·공사 구간·부스트 패드는 `GameJamDefault` 이벤트 목록에서 제외
- [ ] 전용 특수 이벤트 단계 전까지 레거시 자동 이벤트 실행 중단

---

## 3. 새로 구현해야 하는 내용

## P0 — 레이스를 성립시키는 기능

### 3.1 레이스 설정과 상태

- [x] `GameBalanceSettings`와 모든 하위 설정 그룹 구현
- [x] `GameJamDefault`와 `FastTest` 설정 프리셋 구현
- [x] 현재 선택된 프리셋을 공급하는 런타임 설정 제공자 구현
- [x] 기본 차량 6대·1랩·탈락 30초를 `GameJamDefault`에 저장
- [x] 저주 쿨다운 12초를 `GameJamDefault`에 저장
- [x] 부스트 2칸·1.25초 지속·6초 재충전을 `GameJamDefault`에 저장
- [x] 부팅 → 메뉴 → 인트로 → 출발 → 탈락 → 최종 결투 → 승패 → 결과 상태 흐름 — `RacePhase` 6단계 (`GMTKRaceState`)
- [x] 상태 전환 권한을 중앙 레이스 관리자에 집중

### 3.2 부스트

- [x] 충전량·지속시간·재충전·가속 배율을 `BoostSettings`에서 읽음
- [x] `Space` 또는 `Shift` 부스트 입력
- [x] 최대 2칸, 사용 시 한 칸 소비
- [x] 1.25초 추가 가속
- [x] 6초마다 한 칸 재충전
- [ ] 부스트 VFX와 사운드
- [x] 부스트 충전량 HUD
- [x] 추월 성공 시 한 칸 회복
- [x] 엔진 봉인 중 사용과 충전 중단

### 3.3 처형 화면

> 상태: **미구현.** `CameraSettings.executionPlayerViewportRect` 설정만 존재하고 뷰포트 분할·카메라 전환 코드가 없다. GDD 시그니처 요소이므로 우선순위 높음.

- [ ] 시작 시점·Viewport Rect·전환 시간·복귀 시간을 `CameraSettings`에서 읽음
- [ ] 3초 전 처형 대상 카메라 활성화
- [ ] 처형 차량을 메인 화면에 표시
- [ ] 플레이어 카메라를 오른쪽 아래로 이동
- [ ] 플레이어 화면을 약 1/3 크기로 축소
- [ ] 축소 화면에서도 계속 조작 가능
- [ ] 처형 종료 후 플레이어 카메라 정상 복귀
- [ ] 플레이어 자신이 대상일 때 패배 연출로 전환
- [ ] 복수 카메라 렌더링의 WebGL 성능 확인

### 3.4 최종 관문

- [x] 관문 폐쇄 속도·지연·처형 지연·애니메이션 시간을 `PresentationSettings`에서 읽음
- [x] 닫히는 탈출문 오브젝트
- [x] 최종 두 대에서 관문 단계 활성화
- [x] 첫 번째 관문 통과 차량 판정과 승리
- [ ] 승자 뒤에서 관문 즉시 폐쇄
- [x] 패자 즉시 처형
- [ ] 관문 애니메이션과 충돌 판정 동기화
- [ ] 승리 관문 시퀀스

### 3.5 핵심 HUD와 전용 씬

> 상태: 부분 — `RaceHud`(순위·처형 카운트다운·최하위 경고·부스트·저주·도전·관문 라벨)는 구현됐으나 **`ui.unity`에만 배치**되어 `GMTK_Race`에서는 보이지 않는다. 전용 씬(`Assets/Scenes/GMTK_Race.unity`)과 빌드 등록은 완료.

- [ ] 현재 순위와 전체 6대 표시
- [ ] 30초 처형 카운트다운 상시 표시
- [ ] 최하위 경고와 월드 마커
- [ ] 플레이어 최하위 바이탈 경고
- [x] 부스트·내구도·저주·쿨다운 표시
- [ ] 저주 대상 마커
- [x] 퀴즈 문제와 마우스 답 입력
- [ ] 추월 도전 대상과 타이머
- [ ] 처형 경고 → 추월 도전 → 퀴즈 → 쿨다운 순서로 강조
- [ ] 시작 직후 기본 조작 키 가이드
- [ ] 1랩·6대·최종 관문을 포함한 게임 전용 씬
- [ ] 전용 씬 Build Settings 등록

## P1 — 게임의 핵심 정체성

### 3.6 내구도와 대파

> 상태: 구현. 단 `GameJamDefault.maximumDurability`가 임시로 `1000000`이라 실질적으로 대파가 발생하지 않는다.

- [x] 최대 내구도·피해 단계·대파 시간·복구량·보호 시간을 `DamageSettings`에서 읽음
- [x] 차량 내구도 100
- [x] 일반 충돌은 피해 없음 또는 매우 적게 적용
- [x] 강한 충돌과 파열 저주 피해
- [ ] 내구도 60~31 연기·스파크
- [ ] 내구도 30~1 강한 불꽃·엔진 경고
- [x] 내구도 0에서 약 2초 대파
- [x] 내구도 50으로 복구
- [x] 복구 직후 짧은 충돌 보호
- [x] 대파 자체는 직접 탈락시키지 않음
- [ ] 차체 변형 에셋이 없으면 단계별 VFX로 전달

### 3.7 저주 공통 시스템

> 상태: 구현 (`CurseManager`/`CurseController`/`CurseCooldownState`, 공용 쿨다운 12초). 대상 마커·연출 항목은 미검증.

- [x] 저주별 거리·피해·지속시간·밀침·충돌 무시·쿨다운을 `CurseSettings`에서 읽음
- [x] 파열·엔진 봉인·영혼 교환 데이터 정의
- [x] 세 저주가 공유하는 12초 쿨다운
- [x] 시전자보다 순위가 높은 유효 경쟁자 자동 선택
- [ ] 저주 대상 표식
- [x] `E`로 저주 활성화
- [x] 인간 플레이어가 대상이면 퀴즈 시작
- [x] 정답이면 페널티 없이 저주 방어
- [x] 오답·시간 초과면 저주 페널티 적용
- [x] 저주 발동 후 시전자 쿨다운
- [x] 퀴즈 실패 시 적용된 페널티를 화면에 잠시 표시
- [ ] 저주 소유자와 진행 방향을 명확히 표시

### 3.8 파열

> 상태: 구현 (`CurseController` → 대상 내구도 피해 + 밀침).

- [x] 대상 내구도 약 35 감소
- [ ] 짧은 물리적 밀침 또는 피격 반응
- [ ] 플레이어에서 대상으로 이동하는 공격 연출
- [x] 내구도와 대파 시스템 연동

### 3.9 엔진 봉인

> 상태: 구현 (`CurseController` → 대상 부스트 봉인 5초).

- [x] 5초간 대상 부스트 사용 차단
- [x] 5초간 대상 부스트 충전 중단
- [ ] 사슬·봉인·배기구 차단 연출
- [ ] 남은 봉인 시간 아이콘

### 3.10 영혼 교환

> 상태: 구현 (`CurseController.TrySoulSwap`, 최소·최대 거리·충돌 무시 시간 설정 반영).

- [x] 대상 거리 15~40m
- [ ] 양 차량 접지와 유효 트랙 검사
- [ ] 점프·리셋·처형·관문 임계 구간 사용 금지
- [ ] 트랙 진행 거리와 안전한 횡방향 위치 교환
- [ ] 전방 속도 유지와 트랙 방향 정렬
- [ ] 교환 후 약 0.3초 상호 충돌 무시

### 3.11 AI 저주

> 상태: 부분 — AI 저주 발동과 성격별 퀴즈 성공 확률은 구현됐으나 사전 경고·피격 유예·연속 저주 방지는 미구현.

- [ ] AI별 저주 쿨다운·경고시간·피격 유예·성공 확률을 `AISettings`에서 읽음
- [x] AI가 플레이어와 같은 저주 효과 사용
- [x] AI 대상 퀴즈는 화면 없이 능력치 기반 확률로 판정
- [x] AI별 공용 쿨다운과 전방 경쟁자 자동 선택
- [ ] 플레이어가 인지할 수 있는 사전 경고
- [ ] 여러 AI의 피할 수 없는 연속 저주 방지
- [ ] 책략가의 높은 저주 사용 성향

### 3.12 추월 도전

> 상태: 구현 (`OvertakeManager`/`OvertakeChallengeState`, 20초 주기·8초 제한·0.5초 유지·실패 시 감속 페널티).

- [ ] 판정 주기·제한 시간·유지 시간·보상·속박 강도를 `OvertakeSettings`에서 읽음
- [ ] 20초 간격으로 경쟁자 한 대 무작위 선택
- [ ] 이미 플레이어가 앞선 차량이면 해당 도전 무시
- [ ] 대상 차량 카메라와 UI 표시
- [ ] 8초 안에 추월하고 0.5초 유지하면 성공
- [ ] 성공 시 부스트 한 칸 회복
- [ ] 성공 시 저주 쿨다운 즉시 초기화
- [ ] 실패 시 심판의 사슬과 강한 감속
- [ ] 플레이어가 최하위가 되면 즉시 속박 해제
- [ ] 대상·남은 시간·앞섬 여부·실패 결과 표시

## P2 — 시그니처 완성도

### 3.13 처형·카메라 연출

- [ ] 카메라 임계값·FOV·거리·감쇠·흔들림·전환 시간을 `CameraSettings`에서 읽음
- [ ] VFX 크기·지속시간·강도·잔해 유지시간을 `PresentationSettings`에서 읽음
- [ ] 붉은색 또는 지옥식 최하위 외곽선
- [ ] 잠금 → 점화 → 경련 → 폭발 연출
- [ ] 불길·스파크·짧게 남는 잔해
- [ ] 잔해가 트랙을 영구적으로 막지 않게 처리
- [ ] 플레이어 처형 바이탈 사운드
- [ ] 예측형 전방 주시와 속도 기반 거리·FOV
- [ ] 카메라 충돌 회피와 수평선 안정화
- [ ] 옆 차량이 0.6초 유지되면 두 차량 투샷
- [ ] 저주 대상 방향으로 구도 미세 편향
- [ ] 최종 관문에서 두 차량과 관문 프레이밍
- [ ] 급커브·공중·조작 상실 시 기본 카메라 우선

### 3.14 트랙·비주얼·오디오

- [ ] 오디오 볼륨·피치·페이드·경고 단계 수치를 `PresentationSettings`에서 읽음
- [ ] 악마 유해 위 지옥 주조 고속도로 테마
- [ ] 용광로 대로·갈비뼈 코너·제련소·심판의 직선로·관문
- [ ] 부스트와 추월용 넓은 직선 최소 2곳
- [ ] 두 차량 투샷용 병렬 주행 구간 최소 1곳
- [ ] 넓고 읽기 쉬운 도로와 관대한 장벽
- [ ] 불·연기·파티클이 레이싱 라인을 가리지 않게 조정
- [ ] 속도·부스트 연동 엔진 사운드
- [ ] 처형 카운트다운과 최하위 경고음
- [ ] 저주 준비·성공·실패·피격 예고음
- [ ] 추월 도전·관문·폭발·승리 사운드
- [ ] 위험은 빨강, 저주는 청록·보라·녹색, 관문은 흰색·금색 계열
- [ ] 색상뿐 아니라 형태와 움직임으로 상태 구분

### 3.15 주행 특수 이벤트

> 상태: 구현 5종 (`RandomEventManager`: 공사 구간, 가축 횡단, 대형 비치볼, 운석 낙하, 지진). GDD의 덤프트럭 습격은 미구현.

#### 공통 디렉터

- [ ] `SpecialEventDirector`가 `SpecialEventSettings`와 이벤트 정의 목록을 사용
- [ ] 각 이벤트를 프리팹·가중치·경고·효과·오디오가 포함된 데이터 에셋으로 정의
- [ ] 첫 이벤트 지연, 최소·최대 간격, 최대 동시 개수, 레이스당 최대 횟수를 설정에서 읽음
- [ ] 같은 이벤트가 연속으로 선택되지 않도록 설정
- [ ] 처형 경고, 퀴즈, 추월 도전, 최종 결투 중에는 새 이벤트 시작 금지
- [ ] 이미 시작한 이벤트는 상태 전환 시 안전하게 종료하거나 풀로 반환
- [ ] 특수 이벤트는 피해와 순위 손실을 만들 수 있지만 차량을 직접 탈락시키지 않음
- [ ] 플레이어만 강제 타게팅하지 않고 같은 구간의 모든 레이서에게 같은 물리 규칙 적용
- [ ] 모든 런타임 오브젝트는 풀링하고 트랙을 영구적으로 막지 않음

#### 덤프트럭 습격

- [ ] 트랙에 덤프트럭 진입·퇴장 앵커 배치
- [ ] 경적, 진행 방향 화살표, 위험 차선 표시 후 진입
- [ ] 설정 속도로 도로를 가로지르거나 레이싱 라인을 따라 돌진
- [ ] 충돌 차량에 설정 피해와 밀침 적용
- [ ] AI 장애물 감지에 포함
- [ ] 설정 수명 또는 퇴장 앵커 도달 후 풀로 반환

#### 지진

- [ ] 전조음과 지면 균열·먼지 경고
- [ ] 설정 시간 동안 모든 활성 차량에 약한 횡방향 흔들림 적용
- [ ] 카메라 흔들림은 차량 물리와 별도 설정으로 적용
- [ ] 지진 자체는 내구도 피해를 주지 않음
- [ ] 조향 가능성을 유지하고 강제 스핀을 만들지 않음

#### 운석 낙하

- [ ] 레이싱 라인 주변의 유효 충돌 지점을 선택
- [ ] 설정 시간 전에 바닥 원형 마커와 낙하음을 표시
- [ ] 충돌 반경 안의 모든 차량에 설정 피해와 밀침 적용
- [ ] AI도 경고 구역을 피하도록 차선 편향 적용
- [ ] 운석 잔해는 충돌체를 남기지 않고 설정 시간 후 풀로 반환

---

## 4. 선택 기능과 스트레치 골

- [ ] 출발 타이밍 방향키 입력 스타트 부스트
- [ ] 초반 키 가이드 개선
- [ ] 저주 대상 순환 선택
- [ ] 두 번째 추월 도전
- [ ] 경쟁자 이름·초상화·고유 차량
- [ ] 사망 후 관전
- [ ] 추가 퀴즈 문제 유형
- [ ] 점수와 랭크 결과 화면
- [ ] 다음 지옥 층 스토리 장면
- [ ] 추가 카메라 행동
- [ ] 자비·죄인·저주받은 자 난이도
- [ ] 로컬 멀티플레이 또는 비동기 고스트

---

## 5. 게임 잼 비목표

아래 항목은 구현 체크 대상이 아니라 범위 방어용 목록이다.

- 온라인 멀티플레이
- 협동 플레이
- 오픈 월드 탐험
- 사실적인 차량 시뮬레이션
- 깊은 차량 커스터마이징
- 대규모 서사 허브
- 절차적 트랙 생성
- 복잡한 차량 파괴 시스템
- 두 개 이상의 레이스
- 저주 3종을 초과하는 추가 저주
- 완전 자율형 시네마틱 디렉터
- 고급 드리프트와 트릭 시스템

---

## 6. 테스트 체크리스트

### 6.1 규칙과 차량

- [ ] `GameJamDefault`가 아닌 수치의 테스트 프리셋으로도 동일한 규칙 흐름 통과
- [ ] 총 6대가 정상 출발하고 1랩 목표 지점까지 완주
- [ ] 30초마다 최신 최하위 차량 처형
- [ ] 순위 역전 직후에도 올바른 차량 처형
- [ ] 두 대가 남으면 탈락 중단과 관문 결투 시작
- [ ] 관문을 먼저 통과한 차량 승리, 패자 처형
- [ ] 충돌·리스폰·영혼 교환 후에도 순위 정상
- [ ] 키보드만으로 가속·제동·조향·드리프트·부스트 가능
- [ ] 고속 과도한 스핀 없이 충돌 후 빠르게 복구
- [ ] 부스트 충전·소비·봉인 정상
- [ ] 대파 후 약 2초 뒤 정상 복구

### 6.2 처형 화면

- [ ] 3초 전 처형 차량이 메인 화면에 표시
- [ ] 플레이어 화면이 오른쪽 아래 1/3 크기로 축소
- [ ] 축소 화면으로 계속 조작 가능
- [ ] 0초에 대상 차량 폭발
- [ ] 처형 후 플레이어 화면 정상 복귀
- [ ] 플레이어 자신이 대상일 때 패배 흐름 정상
- [ ] 카메라 전환 중 레이싱 라인을 놓치지 않음

### 6.3 퀴즈·저주·도전

- [x] 저주 사용 전에는 퀴즈가 자동 실행되지 않음
- [x] 인간 플레이어가 저주 대상이면 퀴즈가 시작되고 레이스는 계속 진행
- [x] 마우스로 답 선택 가능
- [x] 정답 시 페널티 없이 저주 방어
- [x] 오답·시간 초과 시 페널티 적용과 시전자 쿨다운
- [ ] 영혼 교환 금지 상태에서 안전하게 실패
- [ ] AI 저주 전 경고를 플레이어가 인지
- [ ] 피할 수 없는 연속 AI 저주가 발생하지 않음
- [ ] 추월 도전 성공·실패·속박 해제 정상

### 6.4 주행 특수 이벤트

- [ ] 첫 지연 이후 설정된 최소·최대 간격 안에서 이벤트 시작
- [ ] 같은 이벤트가 연속으로 발생하지 않음
- [ ] 동시에 설정 개수보다 많은 이벤트가 활성화되지 않음
- [ ] 처형 경고·퀴즈·추월 도전·최종 결투 중 새 이벤트 차단
- [ ] 덤프트럭 경고·돌진·충돌·풀 반환 정상
- [ ] 지진이 모든 활성 차량에 같은 규칙으로 적용
- [ ] 운석 경고 지점·반경 피해·밀침 정상
- [ ] 이벤트 피해가 대파는 만들 수 있지만 직접 탈락시키지 않음
- [ ] 재시작 후 진행 중이던 이벤트와 풀 상태 초기화

### 6.5 빌드·성능·플레이테스트

- [ ] 빌드에서 설정 에셋 변경만으로 차량 수·타이머·피해·카메라 수치 변경 가능
- [ ] 코드와 프리팹을 검색해 설정 외부의 중복 밸런스 숫자가 없는지 검증
- [ ] Unity 컴파일 오류 없음
- [ ] EditMode와 PlayMode 테스트 전체 통과
- [ ] Windows 64-bit와 WebGL 빌드 성공
- [ ] 차량 6대·퀴즈·폭발·복수 카메라·특수 이벤트 동시 발생 성능 안정
- [ ] Missing Script·Missing Reference·중복 AudioListener 없음
- [ ] 한 레이스가 3~5분 범위
- [ ] 플레이어가 15초 안에 30초 탈락 규칙 이해
- [ ] 플레이어가 최하위·저주 대상·추월 대상을 식별
- [ ] 신규 플레이어가 일반적으로 첫 탈락까지 생존
- [ ] 카메라가 레이싱 라인을 가리지 않음
- [ ] 최종 관문이 강한 클라이맥스로 느껴짐
- [ ] 외부 플레이테스터 3명이 한 레이스 후 핵심 규칙 설명
- [ ] 결과 직후 재도전 의향 확인

---

## 7. 단계별 구현 계획

각 단계는 독립된 작업 단위다. 해당 단계의 완료 조건과 검증이 모두 통과하기 전에는 다음 단계로 넘어가지 않는다.

### 단계 0 — 작업 기준과 브랜치 정리

**목표:** 모든 구현자가 같은 최신 코드에서 시작하고 사용자 변경을 섞지 않는다.

- [ ] 최신 `develop`을 기준으로 단계별 작업 브랜치 생성
- [ ] 현재 `feature/racing`이 `develop`보다 뒤처진 상태인지 재확인
- [ ] 체크리스트 문서를 구현 브랜치에 포함
- [ ] 작업 전 `git status`에서 사용자 변경과 단계 작업을 구분
- [ ] `Packages/manifest.json`, `Packages/packages-lock.json`의 기존 변경 의도 확인
- [ ] Git 충돌, Unity YAML 충돌, 중복 GUID, 중복 클래스 발생 시 작업을 중단하고 사용자에게 보고

**현재 리뷰 시점 상태:**

- 로컬 `feature/racing`은 `68977d8`이고 `develop`보다 3커밋 뒤지만 독자 커밋은 없다.
- 기준 `develop`은 `cc262db`이며 레이싱과 퀴즈가 모두 포함되어 있다.
- Package 파일 2개에 체크리스트 작업과 무관한 미커밋 변경이 있다.

**완료 조건:**

- [ ] 최신 `develop` 기반 브랜치에서 Unity 컴파일 성공
- [ ] 작업 대상 외 변경이 stage되지 않음
- [ ] 해결되지 않은 충돌 없음

### 단계 1 — 설정 시스템 기반

**목표:** 이후 모든 단계가 숫자와 정책을 코드 수정 없이 변경할 수 있게 한다.

- [ ] `GameBalanceSettings`와 하위 설정 그룹 구현
- [ ] `GameJamDefault`와 `FastTest` 프리셋 생성
- [ ] 선택된 프리셋을 불변 스냅샷으로 공급하는 런타임 제공자 구현
- [ ] 설정 유효성 검사와 구체적인 오류 메시지 구현
- [ ] 숫자 외 정책 설정인 저주 선택·타게팅·추월 보상 모드 구현

**완료 조건:**

- [ ] 프리셋 에셋만 수정해 테스트 타이머와 차량 수가 변경됨
- [ ] 설정 누락이나 잘못된 범위가 시작 전에 검출됨
- [ ] 새 코드에 밸런스 리터럴이 중복되지 않음

**필수 검증:**

- [ ] EditMode 설정 검증 테스트
- [ ] `GameJamDefault`와 `FastTest` 로드 테스트
- [ ] Unity 재컴파일 없이 Inspector 값 변경 후 새 레이스에 반영

### 단계 2 — 1랩·6대 레이스 기본 구성

**목표:** 저주와 탈락 없이도 플레이어 1대와 AI 5대가 한 바퀴를 안정적으로 달린다.

- [ ] 스폰과 레이스 선택기가 `RaceSettings`를 사용하도록 변경
- [ ] AI 5대와 플레이어 1대 스폰
- [ ] 1랩 목표 지점과 체크포인트 구성
- [ ] 게임 전용 레이스 씬 분리 및 Build Settings 등록
- [ ] 레거시 자동 랜덤 이벤트를 특수 이벤트 단계 완료 전까지 비활성화

**완료 조건:**

- [ ] 총 6대가 겹치거나 누락되지 않고 출발
- [ ] 플레이어와 AI가 체크포인트 순서대로 1랩 완주
- [ ] Windows와 WebGL이 같은 게임 씬으로 시작

**필수 검증:**

- [ ] 6대 스폰 PlayMode 테스트
- [ ] AI 5대 완주 스모크 테스트
- [ ] Missing Script·Missing Reference 검사

### 단계 3 — 아케이드 조작감·드리프트·부스트

**목표:** 퀴즈와 전투가 없어도 차량 운전 자체가 안정적이고 재미있다.

- [ ] 저속 조향 보조와 고속 조향 감쇠
- [ ] 스핀 억제와 충돌 후 트랙션 회복
- [ ] 기본 아케이드 드리프트
- [ ] 트랙 이탈과 반대 방향 복구
- [ ] 설정 기반 2칸 부스트와 재충전
- [ ] AI 직선·긴급 회복 부스트

**완료 조건:**

- [ ] 키보드만으로 가속·제동·조향·드리프트·부스트 가능
- [ ] 측면 충돌 후 빠르게 주행 복귀
- [ ] AI 차량이 벽처럼 느껴지지 않음

**필수 검증:**

- [ ] 부스트 소비·재충전·최대치 테스트
- [ ] 드리프트 진입·해제 PlayMode 확인
- [ ] 트랙 이탈·정체·역주행 복구 확인

### 단계 4 — 공용 진행도와 중앙 레이스 상태

**목표:** 순위, 탈락, 저주, 추월, 리스폰이 하나의 진행도와 판정을 공유한다.

- [ ] 체크포인트 구간 진행 거리를 포함한 공용 진행도 구현
- [ ] 동률 안정화와 탈락 차량 제외
- [ ] 레이스 최상위 상태 흐름 구현
- [ ] 기존 `RaceFinish`의 독립 승패 판정 제거 또는 어댑터화
- [ ] 개별 차량이 승리·패배·탈락을 결정하지 못하게 제한

**완료 조건:**

- [ ] 순위표와 모든 규칙 시스템이 같은 진행도 사용
- [ ] 순위가 근접 상황에서 빠르게 깜빡이지 않음
- [ ] 승패 판정 진입점이 중앙 레이스 관리자 하나로 제한됨

**필수 검증:**

- [ ] 랩·체크포인트·구간 거리 순위 테스트
- [ ] 동률과 순위 역전 테스트
- [ ] 충돌·리스폰 후 진행도 테스트

### 단계 5 — 30초 탈락·핵심 HUD·처형 카메라

**목표:** 게임의 핵심인 “30초마다 꼴찌 처형”을 완성한다.

- [ ] 설정 기반 탈락 간격과 10초·5초·3초 경고
- [ ] 0초에 최신 순위를 다시 계산해 최하위 처형
- [ ] 두 대가 남으면 탈락 중단
- [ ] 순위·처형 타이머·최하위 경고 HUD
- [ ] 처형 차량을 메인 화면에 표시
- [ ] 플레이어 화면을 오른쪽 아래 설정 Rect로 축소
- [ ] 처형 후 카메라 정상 복귀

**완료 조건:**

- [ ] 6대에서 네 번의 처형 후 정확히 두 대가 남음
- [ ] 순위 역전 직후에도 올바른 차량이 처형됨
- [ ] 플레이어가 축소 화면으로 계속 조작 가능
- [ ] 플레이어 자신이 대상이면 중복 인셋 없이 플레이어를 메인 화면에 유지하고 0초 처형 시 패배 흐름으로 전환

**필수 검증:**

- [ ] `FastTest` 프리셋으로 네 번 연속 탈락 자동 테스트
- [ ] 경고 단계와 실제 판정 시간이 같은 설정을 사용하는지 확인
- [ ] 복수 카메라 Windows·WebGL 성능 스모크

### 단계 6 — 최종 관문·승패·재시작

**목표:** 두 대가 남은 뒤 실제 목표 지점에서 승자를 결정하는 P0 레이스를 완성한다.

- [ ] 두 대가 남으면 최종 결투 상태로 전환
- [ ] 최종 결투 전에는 일반 결승선이 승패를 발생시키지 않도록 보호
- [ ] 첫 관문 통과 차량 승리
- [ ] 승자 뒤 관문 폐쇄와 패자 처형
- [ ] 승리·패배 결과 화면
- [ ] 모든 런타임 상태를 초기화하는 빠른 재시작

**1랩 구조 안전 규칙:**

- 6대에서 두 대가 되려면 기본 설정 기준 네 번의 처형, 즉 최소 120초가 필요하다.
- [ ] 트랙 최종 진입 예상 시점을 네 번째 처형 이후로 튜닝
- [ ] 차량이 너무 일찍 도착해도 최종 결투 전에는 관문 승패가 발생하지 않게 처리

**완료 조건:**

- [ ] 메뉴부터 결과와 재시작까지 개발자 개입 없이 진행
- [ ] 관문을 먼저 통과한 차량만 승리
- [ ] 재시작 후 이전 차량·타이머·카메라·관문 상태가 남지 않음

**필수 검증:**

- [ ] 선두·후발·동시 진입 관문 판정 테스트
- [ ] 플레이어 승리·패배 PlayMode 테스트
- [ ] 연속 재시작 테스트

### 단계 7 — 내구도와 대파

**목표:** 피해가 직접 탈락시키지 않고 순위 손실을 만드는 규칙을 완성한다.

- [ ] 설정 기반 내구도와 피해 단계
- [ ] 강한 충돌과 파열 피해 진입점
- [ ] 대파 중 조작 상실
- [ ] 설정 시간 후 내구도 복구
- [ ] 복구 직후 충돌 보호
- [ ] 단계별 연기·스파크·불꽃 피드백

**완료 조건:**

- [ ] 내구도 0이 직접 탈락으로 이어지지 않음
- [ ] 대파와 처형 상태가 충돌하지 않음
- [ ] 시각 효과만으로 피해 단계를 구분 가능

**필수 검증:**

- [ ] 피해 임계값·대파·복구·보호 테스트
- [ ] 대파 중 탈락 카운트다운 판정 테스트

### 단계 8 — 플레이어 저주와 퀴즈 연결

**목표:** 운전을 멈추지 않고 퀴즈를 풀어 저주 3종을 사용할 수 있게 한다.

- [x] 자동 퀴즈 반복 실행 중단
- [x] 설정된 저주 선택 방식과 자동 타게팅 적용
- [x] `E` 입력으로 저주 활성화
- [x] 인간 플레이어 대상 퀴즈와 마우스 답안 선택
- [x] 정답 방어·실패 페널티·시전자 쿨다운
- [x] 파열·엔진 봉인·영혼 교환 구현
- [ ] 영혼 교환 안전 조건과 트랙 기반 재배치
- [x] 저주·쿨다운·내구도·부스트 HUD
- [ ] 저주 대상 HUD

**완료 조건:**

- [x] 세 저주가 같은 공용 효과 경로를 사용
- [x] 정답이면 효과 없이 방어하고 오답·시간 초과면 페널티 적용
- [ ] 영혼 교환이 체크포인트와 순위를 망가뜨리지 않음

**필수 검증:**

- [ ] 세 저주 성공·실패 테스트
- [ ] 봉인 중 부스트 사용·재충전 차단 테스트
- [ ] 영혼 교환 허용·금지 상태 테스트
- [x] 퀴즈 중 레이스가 계속되는지 PlayMode 확인

### 단계 9 — AI 성격·부스트·저주

**목표:** AI 5대가 네 가지 성격을 보이며 플레이어와 같은 규칙으로 경쟁한다.

- [ ] 설정 에셋으로 AI 5대 성격 배분
- [ ] 난폭자·스피드스터·책략가·생존자 행동
- [ ] AI 부스트와 순위 회복 판단
- [ ] AI 저주 3종과 사전 경고
- [ ] 연속 저주 방지와 공격 유예
- [ ] 절제된 따라잡기

**완료 조건:**

- [ ] 네 성격의 위협 차이가 플레이에서 보임
- [ ] AI가 눈에 띄는 순간이동 없이 접전 형성
- [ ] 피할 수 없는 연속 저주가 발생하지 않음

**필수 검증:**

- [ ] 성격별 의사결정 테스트
- [ ] AI 저주 경고·유예 테스트
- [ ] 6대 전체 레이스 장시간 스모크

### 단계 10 — 추월 도전

**목표:** 20초 주기의 추월 퀘스트와 심판의 사슬을 완성한다.

- [ ] 무작위 경쟁자 선택과 이미 앞선 차량 무시
- [ ] 대상·제한 시간·앞섬 여부 HUD
- [ ] 8초 안에 추월 후 0.5초 유지 판정
- [ ] 부스트 한 칸 회복과 저주 쿨다운 즉시 초기화
- [ ] 실패 시 속박
- [ ] 최하위가 되면 즉시 속박 해제

**완료 조건:**

- [ ] 성공과 실패가 모두 플레이어에게 명확함
- [ ] 속박 실패가 즉시 확정 사망으로 처리되지 않음
- [ ] 최종 결투 단계에서는 새로운 도전이 시작되지 않음

**필수 검증:**

- [ ] 대상 선택 확률과 무시 조건 테스트
- [ ] 추월 유지시간 경계 테스트
- [ ] 보상 모드와 속박 해제 테스트

### 단계 11 — 주행 특수 이벤트

**목표:** 기존 랜덤 이벤트 프로토타입을 공정하고 설정 가능한 덤프트럭·지진·운석 시스템으로 교체한다.

- [ ] `RandomEventManager`를 `SpecialEventDirector`로 리팩터링
- [ ] 이벤트 정의 데이터 에셋과 트랙 이벤트 앵커 구현
- [ ] 공통 스케줄러·가중치 선택·중복 방지·상태 차단 구현
- [ ] 덤프트럭 습격 구현
- [ ] 지진 구현
- [ ] 운석 낙하 구현
- [ ] 모든 이벤트 오브젝트 풀링
- [ ] 소·비치볼·공사 구간·부스트 패드를 기본 프리셋에서 제외

**완료 조건:**

- [ ] 덤프트럭·지진·운석이 설정 간격으로 주행 중 발생
- [ ] 모든 이벤트에 사전 경고가 있고 회피 또는 대응 가능
- [ ] 처형 경고·퀴즈·추월 도전·최종 결투 중 새 이벤트가 시작되지 않음
- [ ] 이벤트가 차량을 직접 탈락시키거나 트랙을 영구적으로 막지 않음
- [ ] 플레이어와 AI에 동일한 물리·피해 규칙 적용

**필수 검증:**

- [ ] 이벤트 선택 가중치·연속 중복 방지 테스트
- [ ] 상태별 이벤트 차단 테스트
- [ ] 덤프트럭 충돌·지진 흔들림·운석 반경 피해 테스트
- [ ] 이벤트 중 재시작·처형·차량 대파 안전성 테스트
- [ ] Windows·WebGL 풀링과 성능 스모크

### 단계 12 — 카메라·VFX·오디오·지옥 트랙

**목표:** 조작 가독성을 유지하면서 GDD의 지옥 레이스 연출을 완성한다.

- [ ] 속도 기반 카메라 거리·FOV와 충돌 회피
- [ ] 나란히 달릴 때 두 차량 투샷
- [ ] 저주 대상과 최종 관문 프레이밍
- [ ] 잠금·점화·경련·폭발 처형 VFX
- [ ] 지옥 주조 고속도로 랜드마크와 레이싱 라인
- [ ] 엔진·부스트·처형·저주·도전·관문·승리 오디오

**완료 조건:**

- [ ] 특수 카메라가 급커브·공중·조작 상실 때 즉시 해제됨
- [ ] 불·연기·파티클이 도로와 차량을 가리지 않음
- [ ] 잔해가 트랙을 영구적으로 막지 않음

**필수 검증:**

- [ ] 카메라 전환과 중단 조건 PlayMode 테스트
- [ ] 6대·VFX·복수 카메라 성능 테스트
- [ ] 색상 외 형태와 움직임으로 경고 구분 확인

### 단계 13 — 밸런스·외부 테스트·최종 빌드

**목표:** 완료 정의와 Windows·WebGL 출시 기준을 모두 통과한다.

- [ ] 레이스 시간을 3~5분 범위로 조정
- [ ] 신규 플레이어가 첫 탈락까지 생존하도록 초반 난이도 조정
- [ ] 특수 이벤트 빈도와 피해가 처형 긴장감을 방해하지 않도록 조정
- [ ] 외부 플레이테스터 최소 3명 진행
- [ ] 설정만 변경해 밸런스 반복
- [ ] 전체 자동 테스트와 저장소 검증
- [ ] Windows와 WebGL 최종 빌드

**완료 조건:**

- [ ] MVP 완료 조건 전체 체크
- [ ] 외부 테스트 3명이 핵심 규칙 설명 가능
- [ ] Missing Reference·중복 AudioListener·성능 저하 없음
- [ ] 출시 빌드에서 빠른 재시작과 전체 레이스 반복 가능

---

## 8. 기획 일치 및 차이 리뷰

### GDD와 일치하는 핵심

- [x] 1랩, 플레이어 1대와 AI 5대의 총 6대 구성
- [x] 30초마다 최하위 처형, 두 대에서 탈락 중단
- [x] 최종 두 대의 닫히는 관문 결투
- [x] 기본 드리프트와 부스트를 포함한 아케이드 조작
- [x] 내구도 0은 직접 탈락이 아닌 대파
- [x] 파열·엔진 봉인·영혼 교환 3종
- [x] 레이스를 멈추지 않는 마우스 퀴즈
- [x] 20초 주기 추월 도전과 실패 시 심판의 사슬
- [x] 처형 차량 메인 화면과 플레이어 화면 오른쪽 아래 축소
- [x] 지옥 주조 고속도로와 제한적인 시네마틱 카메라

### GDD 내부 모순을 최신안으로 정리한 항목

| 항목 | GDD 표현 | 체크리스트 적용 |
|---|---|---|
| 랩 수 | MVP는 1랩, 트랙 설명은 3랩 | 최신 MVP 기준 1랩 |
| 차량 수 | MVP와 기준표는 6대, 완료 정의는 5대 | 플레이어 1 + AI 5, 총 6대 |
| 퀴즈 시간 | 기준표 3초, 발동 흐름 4초 | 설정 기본값 4초로 확정 |
| AI 수와 유형 | AI 5대, 성격 유형 4개 | 에셋에서 5대에 4유형 배분 |

### 사용자 요청으로 확장된 기획

최신 GDD는 트랙 위험 요소를 절제하라고 했지만, 최종 요청에 따라 아래 세 가지 주행 특수 이벤트를 게임 잼 구현 범위에 포함한다.

| 이벤트 | 구현 범위 | 직접 탈락 |
|---|---|---|
| 덤프트럭 습격 | 경고 후 위험 차선으로 돌진, 충돌 피해와 밀침 | 불가 |
| 지진 | 전조 후 전체 차량에 약한 횡방향 흔들림과 카메라 반응 | 불가 |
| 운석 낙하 | 경고 지점 표시 후 반경 피해와 밀침 | 불가 |

특수 이벤트는 `SpecialEventSettings`로 빈도와 강도를 조절하며 처형 경고, 퀴즈, 추월 도전, 최종 결투와 겹치지 않게 한다. 이 추가는 기존 GDD 대비 명시적인 범위 확장이다.

### GDD에 없지만 구현 안정성을 위해 추가한 항목

| 추가 항목 | 이유 | 게임 디자인 변경 여부 |
|---|---|---|
| `GameBalanceSettings`와 프리셋 | 코드 수정 없이 모든 수치 조정 | 없음 |
| 중앙 레이스 관리자 | 순위·탈락·승패의 중복 판정 방지 | 없음 |
| `FastTest` 프리셋 | 30초를 기다리지 않는 자동 테스트 | 없음 |
| 최종 결투 전 관문 승패 차단 | 1랩 선두가 너무 일찍 완주하는 경우 보호 | 없음 |
| AI 연속 저주 방지 | 플레이테스트의 공정성 요구 충족 | 없음 |

### 최종 확정 정책

- [x] 퀴즈 제한 시간은 설정 기본값 4초
- [x] 저주는 파열 → 엔진 봉인 → 영혼 교환 순서로 순환
- [x] 자동 대상은 가장 가까운 전방 차량, 없으면 가장 가까운 활성 차량
- [x] 추월 성공 보상은 부스트 한 칸과 저주 쿨다운 즉시 초기화
- [x] 퀴즈 표시 언어는 기존 프로젝트 정책대로 영어
- [x] AI는 난폭자 1대, 스피드스터 2대, 책략가 1대, 생존자 1대
- [x] 특수 이벤트는 덤프트럭·지진·운석 세 종류
- [x] 특수 이벤트 수치와 활성 조건은 모두 설정에서 변경

### 주요 설계 리스크

- **1랩 길이:** 총 6대가 두 대가 되려면 기본값으로 120초가 필요하므로 최종 진입이 그보다 빨라서는 안 된다.
- **복수 카메라:** 처형 화면과 플레이어 축소 화면을 동시에 렌더링하므로 WebGL 성능 검증이 필요하다.
- **마우스 퀴즈:** 주행 입력과 포인터 조작을 동시에 요구하므로 버튼 크기와 화면 위치를 조기에 테스트해야 한다.
- **AI 5대와 네 성격:** 중복 배정되는 성격은 프리셋에서 명시해 레이스마다 예측 불가능하게 바뀌지 않게 한다.
- **특수 이벤트 과부하:** 처형·퀴즈·도전과 동시에 발생하면 가독성이 무너지므로 상태 차단 규칙과 최대 동시 개수를 반드시 지킨다.
- **WebGL 이벤트 성능:** 덤프트럭·운석·지진 VFX는 풀링하고 물리 오브젝트 수를 제한해야 한다.

---

## 9. MVP 완료 조건

- [ ] 메뉴부터 결과 화면까지 개발자 개입 없이 진행
- [ ] 레이스가 안정적으로 3~5분 진행
- [ ] 플레이어 1대와 AI 5대가 정상 레이스
- [ ] 30초마다 현재 최하위 차량 처형
- [ ] 충돌·리셋·영혼 교환 중에도 순위와 탈락 판정 신뢰 가능
- [x] 플레이어가 운전 중 저주를 사용하고 인간 대상은 퀴즈로 방어
- [ ] AI가 피할 수 없는 연속 처벌 없이 플레이어 공격
- [ ] 추월 도전 성공과 실패 모두 정상 처리
- [ ] 덤프트럭·지진·운석 특수 이벤트가 사전 경고와 함께 정상 발생
- [ ] 특수 이벤트가 처형·퀴즈·추월 도전·최종 결투 가독성을 방해하지 않음
- [ ] 두 대가 남으면 닫히는 관문 질주로 전환
- [ ] 관문을 먼저 통과한 차량만 승리
- [ ] 승리와 패배가 시각적·기계적으로 명확
- [ ] 재시작이 빠르고 이전 상태가 남지 않음
- [ ] 폭발·차량 6대·UI·복수 카메라·특수 이벤트가 겹쳐도 성능 안정
- [ ] 외부 플레이테스터 3명이 핵심 규칙 설명 가능

---

## 10. 작업 원칙

- 가독성, 레이스 완주 가능성, 30초 최하위 긴장감을 해치는 기능은 단순화하거나 제외한다.
- 문서의 숫자는 설정 기본값일 뿐이며 런타임 코드에 직접 작성하지 않는다.
- 새 숫자 기반 동작을 추가할 때는 먼저 적절한 설정 그룹에 필드를 추가한다.
- UI에 표시되는 시간과 실제 판정 시간은 같은 설정 필드를 사용한다.
- AI와 플레이어에게 같은 효과를 적용할 때는 별도 숫자를 복제하지 않고 공용 효과 설정을 사용한다.
- 특수 이벤트는 차량에 피해와 순위 손실을 줄 수 있지만 직접 탈락 판정을 내리지 않는다.
- 특수 이벤트보다 처형 경고, 퀴즈, 추월 도전, 최종 결투의 가독성을 우선한다.
- P0가 완성되기 전에는 스트레치 작업을 우선하지 않는다.
- 개별 차량이 탈락·승리·관문 결과를 독립적으로 판정하지 않는다.
- 구현 완료 후 테스트 결과와 함께 체크박스를 갱신한다.
- Git 병합 충돌, Unity YAML 충돌, 중복 클래스, 중복 GUID가 발생하면 임의로 해결하지 않고 충돌 내용과 권장안을 먼저 팀에 공유한다.
